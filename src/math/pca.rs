//!
//! Principle Component Analysis
//!
use matrix_kit::dynamic::matrix::Matrix;

// /// Computes PCA of a list of vectors, which represent some data points!
// pub fn pca(data: Vec<Matrix<f64>>) -> Matrix<f64> {
//     // Turn these data points into some sort of matrix
//     let data_matrix = Matrix::from_cols(data).transpose();

//     data_matrix.svd()
// }
